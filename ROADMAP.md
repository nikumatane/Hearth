<p align="center">
  <strong>简体中文</strong>&nbsp;&nbsp;|&nbsp;&nbsp;<a href="ROADMAP.en.md">English</a>
</p>

# Hearth 路线图

本页记录已经确认的三个公开迭代边界。版本范围发生变化时，应先更新本页，再进入实现，
避免把尚未完成的适配器或高风险升级能力提前暴露给用户。

## 1.1.0 · 开源与游戏管理基础

状态：已完成（仓库公开时间由维护者另行决定，不阻塞后续版本开发）。

- 采用 MIT 许可证，并建立贡献、安全报告、持续集成和可复现发布等开源基础。
- Hearth 启动时只做有边界的只读探测，不自动下载、安装、接管或启动游戏。
- 没有发现可管理游戏时，管理员主页显示首次启动向导；成员只看到尚未配置的状态。
- 至少已有一个游戏时，主页保持正常，管理员从独立的“系统设置 / 游戏管理”页面管理
  已安装、可接管和可安装游戏。
- 所有接管和安装均由管理员明确发起，选择目录并确认后才执行；已有文件和存档默认不改动。
- 首个正式安装提供者为 Palworld。DST 可以被识别并显示后续支持状态，但在生产适配器完成前
  不提供安装按钮。
- 后台设置采用校验、修订号、原子写入和上一版回退文件；敏感值只能替换，不能读取明文。

## 1.2.0 · 面板安全自更新

状态：已完成。

- 管理员可在面板中检查 stable / prerelease 更新，但不会静默安装。
- 下载 GitHub Release 的版本化 Windows 包，验证摘要与发布来源后再进入升级。
- 使用独立 updater 停止 Hearth 计划任务、替换程序、重新启动并执行健康检查。
- 健康检查失败时自动恢复旧程序和原配置；更新过程、结果和回滚均进入审计。
- 游戏进程与存档不参与面板程序替换，不因 Hearth 自更新而被停止或修改。

## 1.3.0 · DST 正式适配器

状态：已完成（Windows；当前阶段不包含 DST 自动安装、模组和备份恢复）。

- 第一阶段已支持现有 Don't Starve Together Dedicated Server 与 cluster 的只读探测、管理员接管，
  以及 Master/Caves 双分片的启动、停止和重启；安装、模组和备份能力仍在后续阶段。
- 以一个逻辑游戏实例管理 Master / Caves 分片的进程、端口、日志和生命周期。
- 管理 cluster 配置、token 状态、模组更新、安全关闭、备份和恢复边界；管理员可在游戏管理页
  写入 DST Token，第一阶段不读取或回显 cluster token 明文，且停止/重启必须明确确认强制终止。
- 复用 1.1.0 的游戏目录、安装任务和后台设置能力，不为 DST 建立旁路实现。

## 1.3.1 · 游戏管理页与 DST 配置

状态：已实现，随 1.3.2 继续验收。

- 帕鲁和 DST 的配置入口只对“本机已安装且已由 Hearth 接管”的游戏显示；未安装、仅发现或未接管时不显示配置导航。
- 为 DST 增加管理员专属的服务器配置与 Master/Caves 世界规则，并保留三个 INI 和两个
  `worldgenoverride.lua` 的高级编辑；世界规则只做静态解析和白名单增量写入，不自动重建存档。
- 沿用修订校验、停服写入、敏感值掩码和审计边界；无法安全解析的自定义 Lua 只允许管理员
  回到高级文件处理，不阻塞其他 DST 配置读取。
- 不把 DST 配置误归入 Palworld 的 WorldOption/INI 编辑器，也不向成员开放。

## 1.3.2 · 通用游戏版本管理

状态：已实现，随 1.3.3 继续验收。

- 抽象游戏适配器的当前版本、版本来源、检查状态和可用版本字段，详情页统一展示，不再只支持 Palworld。
- Palworld 继续使用实际服务端 depot/manifest；DST 使用其 SteamCMD App/depot 版本，不展示 SteamCMD 自身版本。
- 版本检查保持低频、可手动触发、可审计，并与游戏是否已接管的状态明确区分。

## 1.3.3 · DST 版本检查与安全更新

状态：开发中（当前分支 `release/1.3.3`）。

- 接入 DST 的 SteamCMD 版本检查与更新任务，复用现有备份、阶段进度、无进展超时、日志和失败恢复能力。
- 更新前按 Master/Caves 生命周期执行停服、一致性备份、更新、按原状态启动和健康确认；失败时
  保留新备份并尽力恢复任务前运行状态。
- 只有已安装且已接管的 DST 才显示更新入口；不自动安装游戏、不处理模组。

## 版本规则

- 以上迭代默认保持现有配置和 HTTP API 向后兼容。
- 只有必须破坏配置结构、安装模型或公开 API 时才进入 2.0.0，并提供迁移与回滚说明。
- 未达到对应版本完整验收条件的功能，应保持隐藏或明确标记为“计划支持”，不能以可用状态展示。
