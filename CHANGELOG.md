<p align="center">
  <strong>简体中文</strong>&nbsp;&nbsp;|&nbsp;&nbsp;<a href="CHANGELOG.en.md">English</a>
</p>

# Hearth 版本变更

本文件从 `0.8.0` 开始记录面向使用者的变化。开发分支中尚未发布的内容放在
“未发布”一节，构建正式安装包时再归入对应版本。

## 未发布

- 管理员“系统设置 → 面板更新”新增 stable / prerelease 版本检查与手动确认安装；不会静默更新。
- 面板更新固定从 `nikumatane/Hearth` GitHub Release 下载版本化 Windows 包，依次校验
  Release 制品摘要、随包 SHA256、文件名与包内 `VERSION`。私有仓库阶段支持仅从受保护
  文件或环境变量读取 Contents: Read Token，API 与页面不返回明文。
- 新增随包独立 `hearth-updater.exe`：等待旧面板退出后替换程序、重新运行 `Hearth` 计划
  任务并检查新版本健康状态；失败时自动恢复旧程序。游戏进程、存档和游戏配置不参与替换。
- 更新检查、开始、成功、失败和自动回滚进入管理员安全操作审计；跨面板重启的结果通过
  受保护状态文件一次性导入，详细运行记录保存在 `panel-update.log`。
- 后台设置可选择更新通道，保存使用既有修订校验和原子配置替换，重启 Hearth 后生效。

## 1.1.0 - 2026-08-04

- 启动改为支持零游戏配置：只做有界只读探测，不因 Palworld 缺失而终止 Hearth。
- 管理员零游戏主页新增首次启动向导；已有游戏后的探测、接管、安装和后台参数集中到独立
  “系统设置”页面，成员不能访问。
- Palworld 支持管理员明确确认后的现有安装接管和全新安装。新安装使用独立暂存目录下载
  SteamCMD，校验 ZIP 路径/体积，并固定使用 SteamCMD 标准 `steamapps\\common\\PalServer`
  目录；完成后不自动启动服务器。
- Windows 启动改为直接运行 `PalServer-Win64-Shipping-Cmd.exe` 及官方多线程参数，避开部分
  SteamCMD 安装中 `PalServer.exe` 包装器无法拉起实际服务端的问题；安装和更新完成后等待
  SteamCMD 派生进程退出，避免后续文件冲突。
- 安装任务从 SteamCMD 日志读取自身更新与 Palworld 下载百分比，映射到既有阶段进度并保证
  进度不倒退，不增加不可靠的速度或剩余时间估算。
- DST 只做只读识别并标记为 1.3.0 计划支持，不提前开放安装或接管。
- 后台配置新增管理路径、备份保留、SteamCMD 无进展超时、端口、Cookie 和可信代理设置；
  保存采用修订校验、原子替换并保留上一版配置；游戏接管、安装任务启动和后台设置保存进入
  管理员安全操作审计。
- 项目采用 MIT 许可证，并增加中英文路线图、贡献指南、安全报告策略和 GitHub Actions CI 基线。

## 1.0.4 - 2026-08-03

- 帕鲁详情页新增真实在线玩家昵称列表；面板 API 只返回清理后的显示名，不暴露 REST
  响应中的平台账号、Player ID 或 User ID。“玩家数据”同时改名为更明确的“人数来源”。
- 玩家上限优先使用运行中 REST metrics 的 `maxplayernum`。REST 不可用且当前世界存在
  `WorldOption.sav` 时明确显示上限未知，不再错误回退到可能已失效的 INI 数值。

## 1.0.3 - 2026-08-02

- 修复 Steam App 顶层 Build ID 变化但 Palworld Dedicated Server depot 未变化时持续误报
  “服务端有更新”的问题；版本检查改为比较本机与 public 分支的实际 depot manifest。
- SteamCMD 返回 App `2394010` 已是最新版时，更新任务明确显示“服务器已是最新版”并清除
  旧更新提示，不再把 SteamCMD 后续的自身校验输出误认为 Palworld 下载。
- 任务日志改为与每次操作记录一一关联并按需读取；面板日志常驻，运行中任务自动打开，
  历史任务从操作记录进入，非面板日志标签可以关闭，配置保存等无日志操作不显示入口。

## 1.0.2 - 2026-08-02

- 修复 `WorldOption.sav` 浮点数 `5` 被序列化为 `5.0` 后误报往返校验失败的问题，同时
  保留未知字段、结构和非浮点值的严格完整校验。
- 服务端版本检查拆分为 SteamCMD 准备和 Palworld App `2394010` 查询两个阶段；页面不再
  并排展示不同格式的游戏版本与 Build ID，并新增六小时低频自动检查。
- 安全关闭会刷新在线人数：无人在线时使用 5 秒通知，有人在线或人数不可用时保留原配置
  的完整等待时间；任务阶段显示剩余关闭时间。

## 1.0.1 - 2026-08-01

- 登录与攻击审计不再混入 IP 规则操作；旧版混合记录保留在原文件，但规则事件不会继续
  作为登录记录显示，也不能从规则操作行误操作操作者 IP。
- 新增独立的管理员安全操作审计，结构化区分操作者来源 IP 与操作目标，覆盖成员凭据的
  创建、修改、删除和 IP 黑白名单的添加、删除；不记录密码、摘要或其他敏感凭据。
- 安全操作审计使用独立 `operation-audit.jsonl`，保留最近 1000 条，达到 5 MiB 后轮转
  并保留一代；Windows 升级安装会自动配置并保留该文件。

## 1.0.0 - 2026-07-31

- Palworld 详情页“当前版本”新增按需检查入口，通过 SteamCMD 比较本机与 public
  分支 Build ID，并在现有任务区展示阶段进度。
- 总览问候语从固定文案改为访问者浏览器本地时间，并每分钟更新日期与时段。
- 同步架构文档中的无 REST 停止/重启安全边界。
- 成员配置权限收敛为后端白名单中的 `PalWorldSettings.ini` 日常玩法参数；系统、安全、
  性能参数和完整 `WorldOption.sav` 保持管理员专属，旧权限升级时自动安全降级。
- 新增管理员参数审计：记录成功保存的操作者、来源 IP、配置版本和真实参数差异；敏感值
  不落盘，JSONL 达到 5 MiB 后自动轮转。
- 收紧复合 INI 值结构校验，避免夹带额外参数；登录限流改为按来源 IP，限制会话表大小，
  并为登录审计增加 5 MiB 单代轮转。
- Palworld ZIP 备份新增按时间与总容量双阈值保留，默认 30 天/20 GiB，始终保留刚创建
  的备份且不清理未知文件。
- SteamCMD 正式更新改为 30 分钟无日志进展超时；自身更新日志会刷新计时，自更新后未
  出现 Palworld 完成标志时自动重试一次，并在 Windows 上按进程树终止卡死任务。
- 登录保护升级为分来源指数退避和有界 PBKDF2 并发；可信设备使用签名 Cookie 和保留
  校验通道，但仍必须输入正确密码，不能免密创建会话。
- 登录审计新增疑似自动化/暴力破解等级、连续失败次数和 IP 规则变更；三点菜单可以把
  精确 IP 快速加入黑名单 24 小时或白名单 7 天。
- 新增管理员 IP 黑白名单页。黑名单在密码计算前拒绝，白名单只影响限流不跳过密码；
  支持到期时间、永久规则、备注和运行时命中统计。
- 转发头改为仅信任 `trustedProxyCidrs` 中的直接代理，并从右向左解析代理链，防止客户端
  伪造来源 IP 或 HTTPS Cookie 标记。Windows 默认只信任本机回环代理。
- 安装器管理员密码和新建/修改的成员密码统一要求至少 10 个字符；已有成员摘要继续可用，
  并建议使用更长且唯一的密码。

## 0.8.0

- REST API 不可用时，停止和重启可在用户明确确认存档风险后回退为终止已捕获的
  Palworld 进程；PID 或进程启动时间变化时拒绝误杀。
- 启动、停止、重启、更新和备份任务新增阶段、细节与百分比进度。
- 成员可以在总览和详情页看到自己有权执行的任务进度，任务日志与登录审计仍仅管理员可见。
- 完善手机宽度下的导航、确认框、任务状态和操作布局。
- Windows 安装包、健康检查和页面版本标识统一为 `0.8.0`。
