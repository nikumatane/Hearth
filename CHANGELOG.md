# Hearth 版本变更

本文件从 `0.8.0` 开始记录面向使用者的变化。开发分支中尚未发布的内容放在
“未发布”一节，构建正式安装包时再归入对应版本。

## 未发布

暂无。

## 1.0.0 - 2026-07-31

- Palworld 详情页“当前版本”新增按需检查入口，通过 SteamCMD 比较本机与 public
  分支 Build ID，并在现有任务区展示阶段进度。
- 总览问候语从固定文案改为访问者浏览器本地时间，并每分钟更新日期与时段。
- 同步架构文档中的无 REST 停止/重启安全边界。
- 成员配置权限收敛为后端白名单中的 `PalWorldSettings.ini` 日常玩法参数；系统、安全、
  性能参数和完整 `WorldOption.sav` 保持管理员专属，旧权限升级时自动安全降级。
- 新增管理员参数审计：记录成功保存的操作者、来源 IP、配置版本和真实参数差异；敏感值
  不落盘，JSONL 达到 5 MiB 后自动轮转。
- 收紧复合 INI 值结构校验，避免夹带额外参数；登录限流改为按来源 IP，限制会话表大小，
  并为登录审计增加 5 MiB 单代轮转。
- Palworld ZIP 备份新增按时间与总容量双阈值保留，默认 30 天/20 GiB，始终保留刚创建
  的备份且不清理未知文件。
- SteamCMD 正式更新改为 30 分钟无日志进展超时；自身更新日志会刷新计时，自更新后未
  出现 Palworld 完成标志时自动重试一次，并在 Windows 上按进程树终止卡死任务。
- 登录保护升级为分来源指数退避和有界 PBKDF2 并发；可信设备使用签名 Cookie 和保留
  校验通道，但仍必须输入正确密码，不能免密创建会话。
- 登录审计新增疑似自动化/暴力破解等级、连续失败次数和 IP 规则变更；三点菜单可以把
  精确 IP 快速加入黑名单 24 小时或白名单 7 天。
- 新增管理员 IP 黑白名单页。黑名单在密码计算前拒绝，白名单只影响限流不跳过密码；
  支持到期时间、永久规则、备注和运行时命中统计。
- 转发头改为仅信任 `trustedProxyCidrs` 中的直接代理，并从右向左解析代理链，防止客户端
  伪造来源 IP 或 HTTPS Cookie 标记。Windows 默认只信任本机回环代理。
- 安装器管理员密码和新建/修改的成员密码最低长度提高到 14 个字符；已有成员摘要继续可用。

## 0.8.0

- REST API 不可用时，停止和重启可在用户明确确认存档风险后回退为终止已捕获的
  Palworld 进程；PID 或进程启动时间变化时拒绝误杀。
- 启动、停止、重启、更新和备份任务新增阶段、细节与百分比进度。
- 成员可以在总览和详情页看到自己有权执行的任务进度，任务日志与登录审计仍仅管理员可见。
- 完善手机宽度下的导航、确认框、任务状态和操作布局。
- Windows 安装包、健康检查和页面版本标识统一为 `0.8.0`。
